# s16_mul_s16_u16_fac025

## Scope

P0

source: `src_analysis/sources/mul_Ccode.c`  
header: `src_analysis/headers/mul_Ccode.h`  
harness: `harnesses/P0/eva_main_s16_mul_s16_u16_fac025.c`  
machdep: `ppc_32`

## Analyzed interface

`s16 s16_mul_s16_u16_fac025(s16, u16);`

## Input model

برای هر دو پارامتر کل دامنه نوع آن‌ها در نظر گرفته شد:

- `value`: از `-32768` تا `32767`
- `factor`: از `0` تا `65535`

هیچ محدودیت یا رابطه‌ای میان ورودی‌ها اعمال نشد.

Eva ورودی‌ها را به‌صورت abstract تحلیل می‌کند.

## Baseline result

Eva با optionهای زیر اجرا شد:

`-eva -eva-slevel 1000 -eva-ilevel 256`

در عملیات عددی helper، هشدار overflow، division by zero یا narrowing گزارش نشد.

### Missing return

تابع:

`s16_mul_s16_u16_fac025`

محل:

`src_analysis/sources/mul_Ccode.c:3344`

تابع مقدار برگشتی helper را return نمی‌کند و تابع non-void بدون دستور `return` به انتهای body می‌رسد.

در نتیجه، پس از فراخوانی تابع هدف، مسیر عادی اجرا به ادامه harness نرسید.

## Note

این گزارش مربوط به وضعیت فعلی source و پیش از اصلاح missing return است.

ثابت scaling برابر `262144` است و helper مقدار محاسبه‌شده را در بازه نوع `s16` برمی‌گرداند، اما wrapper فعلی این مقدار را return نمی‌کند.

در این baseline هیچ patch رفتاری روی wrapper انجام نشده است.
