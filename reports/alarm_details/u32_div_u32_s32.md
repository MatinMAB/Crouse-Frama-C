# u32_div_u32_s32

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_u32_div_u32_s32.c`  
machdep: `ppc_32`

## Analyzed interface

`u32 u32_div_u32_s32(u32 numerator, s32 denominator);`

## Input model

پارامترها با کل دامنه نوع خود مدل شدند:

```text
numerator   = 0..4294967295
denominator = -2147483648..2147483647

مقادیر صفر، -1 و -2147483648 از دامنه denominator حذف نشدند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse بدون warning یا error انجام شد.

Summary رسمی Eva ثبت کرد:

0 alarms generated by the analysis.

هیچ alarm مربوط به overflow، division by zero، invalid shift یا invalid memory access ثبت نشد.

Final state
u32Temp1 = full u32 domain or UNINITIALIZED
return   = full u32 domain

تابع هدف و eva_main دارای normal final state بودند و propagation پس از فراخوانی ادامه پیدا کرد.

Baseline boundary

هیچ تغییری در بررسی denominator، عملیات signed و unsigned، division، negation، shiftها، branchها یا returnهای تابع انجام نشد.
