# u32_div_s32_u32

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_u32_div_s32_u32.c`  
machdep: `ppc_32`

## Analyzed interface

`u32 u32_div_s32_u32(s32 numerator, u32 denominator);`

## Input model

پارامترها با کل دامنه نوع خود مدل شدند:

```text
numerator   = -2147483648..2147483647
denominator = 0..4294967295

Builtinهای استفاده‌شده:

Frama_C_long_interval(
    -2147483648,
    2147483647)

Frama_C_unsigned_long_interval(
    0,
    4294967295)

مقدار صفر از دامنه denominator حذف نشد.

مقادیر INT32_MIN، INT32_MAX و UINT32_MAX در دامنه ورودی‌ها حفظ شدند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse با موفقیت و بدون warning یا error انجام شد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

0 alarms generated by the analysis.

Eva برای تابع هدف و Harness normal final state تولید کرد.

Final-state note

Final state تابع هدف:

s32Temp1 = [-2147483648..2147483646],0%2
return   = full u32 domain

Final state Harness نشان داد که ورودی‌ها و متغیر result به interval محدودتری از دامنه نوع خود محدود نشدند.

Propagation پس از فراخوانی تابع ادامه پیدا کرد و eva_main مقدار صفر برگرداند.

Baseline boundary

این گزارش وضعیت implementation فعلی را ثبت می‌کند.

هیچ تغییری در موارد زیر انجام نشد:

بررسی denominator صفر
بررسی علامت numerator
conversionهای signed و unsigned
divisionها
multiplication
subtraction
left shift
addition
branchها
returnها
