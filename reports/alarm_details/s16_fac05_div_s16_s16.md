# s16_fac05_div_s16_s16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_s16_fac05_div_s16_s16.c`  
machdep: `ppc_32`

## Analyzed interface

`s16 s16_fac05_div_s16_s16(s16 numerator, s16 denominator);`

## Input model

هر دو پارامتر با کل دامنه نوع `s16` مدل شدند:

```text
numerator   = -32768..32767
denominator = -32768..32767

برای هر ورودی از builtin زیر استفاده شد:

Frama_C_short_interval(-32768, 32767)

مقدار صفر از دامنه denominator حذف نشد و مقدار -32768 نیز در دامنه هر دو ورودی باقی ماند.

هیچ assumption، cast، mask یا محدودسازی اضافه ای در Harness اعمال نشد.

## Baseline result

Parse با موفقیت و بدون warning یا error انجام شد.

Eva برای تابع هدف و Harness normal final state تولید کرد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

11 alarms generated by the analysis.

ترکیب alarmها:

6 integer overflows
3 divisions by zero
2 invalid shifts
Alarm locations
Line 173

یک alarm برای نامعتبر بودن operand سمت چپ left shift در عبارت زیر ثبت شد:

numerator << 16

Line 190

شش alarm برای محاسبه division، multiplication، subtraction و left shift ثبت شد:

division by zero
دو signed overflow برای multiplication
invalid operand برای left shift
signed overflow برای subtraction
signed overflow برای نتیجه left shift
Line 191

یک alarm مربوط به division by zero ثبت شد.

Line 192

سه alarm ثبت شد:

division by zero
دو signed overflow برای addition
Final-state note

Eva final state زیر را برای تابع هدف ثبت کرد:

s32Temp1 = full s32 domain
s32Temp2 = [0..2147483646],0%2 or UNINITIALIZED
return   = full s16 domain

در final state مربوط به Harness، دامنه numerator به شکل زیر گزارش شد:

numerator = [0..32767]

مقدار بازگشتی تابع و متغیر result در Harness به interval محدودتری از دامنه نوع خود محدود نشدند.

Propagation ادامه پیدا کرد و eva_main با مقدار بازگشتی صفر به normal final state رسید.

Reachability note

Eva تعداد 28 statement از 49 statement موجود در توابع تحلیل شده را reachable گزارش کرد.

این Baseline برای مسیرهایی که بعد از diagnosticهای عملیات signed ادامه داده شدند، نتیجه تحلیل را ثبت می کند.

## Baseline boundary

این گزارش وضعیت implementation فعلی را ثبت می کند.

هیچ تغییری در موارد زیر انجام نشد:

left shift اولیه numerator
بررسی denominator صفر
division
multiplication
subtraction
left shift remainder
addition
branchها
returnها
