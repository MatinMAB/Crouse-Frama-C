# u16_sin_u16

## Scope

P1

source: `src_analysis/sources/trig_Ccode.c`  
header: `src_analysis/headers/trig_Ccode.h`  
harness: `harnesses/P1/eva_main_u16_sin_u16.c`  
machdep: `ppc_32`

## Analyzed interface

`u16 u16_sin_u16(u16 Phi);`

## Input model

پارامتر `Phi` با کل دامنه نوع `u16` مدل شد:

`0..65535`

برای تولید ورودی abstract از builtin زیر استفاده شد:

`Frama_C_unsigned_short_interval(0, 65535)`

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

## Baseline result

Parse با موفقیت انجام شد.

برای تابع هدف warning مربوط به fall-through یا missing return ثبت نشد. هر دو مسیر اصلی تابع مقدار بازگشتی دارند.

Eva مسیر مستقیم `Phi == 0` و مسیر محاسباتی `Phi != 0` را تحلیل کرد و normal final state برای تابع و Harness تولید شد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

`0 alarms generated by the analysis.`

هیچ assertion نامعتبر یا unknown برای تابع هدف ثبت نشد و precondition مربوط به builtin ورودی معتبر بود.

## Precision note

در زمان تحلیل loop، Eva شروع به merge کردن iterationها کرد.

در final state، مقدار بازگشتی تابع و متغیر `result` به بازه دقیق‌تری محدود نشدند و در دامنه کامل نوع `u16` باقی ماندند:

`0..65535`

بنابراین این Baseline نبود alarm تولیدشده با تنظیم فعلی را ثبت می‌کند، اما بازه عددی دقیقی برای خروجی sine ارائه نمی‌دهد.

## Active behavior

پیاده‌سازی فعال شامل موارد زیر است:

- شاخه جداگانه برای `Phi == 0`
- loop با حداکثر 16 iteration
- دسترسی به `arctantab[u16Ind + 1]`
- right shiftهای متغیر با `u16Ind`
- arithmetic روی مقادیر `u16`، `u32` و `s32`
- right shift نهایی با مقدار ثابت 14

برای این عملیات، Eva در Baseline فعلی alarmی درباره overflow، shift نامعتبر، index خارج از محدوده یا دسترسی حافظه نامعتبر تولید نکرد.

هیچ تغییری در implementation تابع، جدول، loop، arithmetic یا returnها انجام نشد.
