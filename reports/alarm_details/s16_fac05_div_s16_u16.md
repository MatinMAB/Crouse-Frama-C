# s16_fac05_div_s16_u16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_s16_fac05_div_s16_u16.c`  
machdep: `ppc_32`

## Analyzed interface

` s16 s16_fac05_div_s16_u16(s16 numerator, u16 denominator);`

## Input model

پارامترها با کل دامنه نوع خود مدل شدند:

```text
numerator   = -32768..32767
denominator = 0..65535

مقدار صفر از دامنه denominator حذف نشد و مقادیر منفی numerator نیز حفظ شدند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse بدون warning یا error انجام شد.

Summary رسمی Eva ثبت کرد:

5 alarms generated by the analysis:
3 integer overflows
2 invalid shifts
Alarm locations
Line 348

یک invalid operand برای left shift روی مقدار حاصل از تبدیل numerator ثبت شد.

Line 362

یک signed overflow برای multiplication ثبت شد.

Line 363

یک invalid operand برای left shift و یک signed overflow برای نتیجه shift ثبت شد.

Line 365

یک signed overflow برای حد بالای addition ثبت شد.

Final state
u16Temp1 = full u16 domain
s32Temp1 = 0..2147483647
s32Temp2 = even values in 0..2147483646 or UNINITIALIZED
return   = 0..32767

در final state تابع، دامنه numerator به 0..32767 محدود شده است.

تابع هدف و eva_main دارای normal final state بودند و propagation پس از فراخوانی ادامه پیدا کرد.

Baseline boundary

هیچ تغییری در left shift اولیه، بررسی denominator، division، multiplication، محاسبه remainder، shift دوم، addition، محدودسازی خروجی، branchها یا returnها انجام نشد.
