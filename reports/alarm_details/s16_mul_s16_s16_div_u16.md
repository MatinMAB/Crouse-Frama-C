# s16_mul_s16_s16_div_u16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_s16_mul_s16_s16_div_u16.c`  
machdep: `ppc_32`

## Analyzed interface

`s16 s16_mul_s16_s16_div_u16(s16 data1, s16 data2, u16 denominator);`

## Input model

هر سه پارامتر با کل دامنه نوع خود مدل شدند:

```text
data1       = -32768..32767
data2       = -32768..32767
denominator = 0..65535

Builtinهای استفاده‌شده:

Frama_C_short_interval(-32768, 32767)
Frama_C_unsigned_short_interval(0, 65535)

مقدار صفر از دامنه denominator حذف نشد و مقدار -32768 نیز در دامنه هر دو ورودی signed باقی ماند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse با موفقیت و بدون warning یا error انجام شد.

Eva برای تابع هدف و Harness normal final state تولید کرد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

7 alarms generated by the analysis.

ترکیب alarmها:

6 integer overflows
1 invalid shift
Alarm locations
Line 1022

شش alarm برای محاسبه quotient، multiplication، remainder و left shift ثبت شد:

دو signed overflow برای multiplication
نامعتبر بودن operand سمت چپ left shift
دو signed overflow برای subtraction
signed overflow برای نتیجه left shift
Line 1023

یک signed overflow برای addition دو quotient ثبت شد.

Final-state note

Eva final state زیر را برای تابع هدف ثبت کرد:

s32Result = [-1073709056..2147483647]
s32Temp1  = [0..2147483646],0%2
return     = full s16 domain

مقدار بازگشتی تابع و متغیر result در Harness به interval محدودتری از دامنه نوع خود محدود نشدند.

Propagation ادامه پیدا کرد و eva_main با مقدار بازگشتی صفر به normal final state رسید.

Baseline boundary

این گزارش وضعیت implementation فعلی را ثبت می‌کند.

هیچ تغییری در موارد زیر انجام نشد:

ضرب data1 و data2
بررسی denominator صفر
divisionها
multiplication quotient در denominator
محاسبه remainder
left shift remainder
addition نتایج میانی
saturation خروجی
branchها
returnها
