# u16_mul_s16_u16_div_s16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_u16_mul_s16_u16_div_s16.c`  
machdep: `ppc_32`

## Analyzed interface

`u16 u16_mul_s16_u16_div_s16(s16 factor1, u16 factor2, s16 denominator);`

## Input model

پارامترها با کل دامنه نوع خود مدل شدند:

```text
factor1     = -32768..32767
factor2     = 0..65535
denominator = -32768..32767

مقادیر صفر، -1 و -32768 از دامنه denominator حذف نشدند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse بدون warning یا error انجام شد.

Summary رسمی Eva ثبت کرد:

11 alarms generated by the analysis.

ترکیب alarmها:

7 integer overflows
3 divisions by zero
1 invalid shift
Alarm locations
Line 740

هفت alarm ثبت شد:

یک division by zero
دو signed overflow برای multiplication
یک invalid operand برای left shift
دو signed overflow برای subtraction
یک signed overflow برای نتیجه left shift
Line 741

چهار alarm ثبت شد:

دو division by zero
دو signed overflow برای addition
Final state
u32Result = {0}
u32Temp1  = {0}
s32Result = full s32 domain
s32Temp1  = [0..2147483646],0%2
return    = full u16 domain

تابع هدف و eva_main دارای normal final state بودند و propagation پس از فراخوانی ادامه پیدا کرد.

Baseline boundary

هیچ تغییری در ضرب اولیه، بررسی denominator، divisionها، multiplication، محاسبه remainder، left shift، addition، saturation، branchها یا returnها انجام نشد.
