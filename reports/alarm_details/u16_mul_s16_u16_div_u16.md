# u16_mul_s16_u16_div_u16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_u16_mul_s16_u16_div_u16.c`  
machdep: `ppc_32`

## Analyzed interface

`u16 u16_mul_s16_u16_div_u16(s16 factor1, u16 factor2, u16 denominator);`

## Input model

پارامترها با کل دامنه نوع خود مدل شدند:

```text
factor1     = -32768..32767
factor2     = 0..65535
denominator = 0..65535

مقدار صفر از دامنه denominator حذف نشد و مقدار -32768 در دامنه factor1 باقی ماند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse بدون warning یا error انجام شد.

Summary رسمی Eva ثبت کرد:

7 alarms generated by the analysis.

ترکیب alarmها:

6 integer overflows
1 invalid shift
Alarm locations
Line 795

شش alarm برای multiplication، subtraction و left shift ثبت شد:

دو signed overflow برای multiplication
یک invalid operand برای left shift
دو signed overflow برای subtraction
یک signed overflow برای نتیجه left shift
Line 796

یک signed overflow برای حد بالای addition ثبت شد.

Final state
s32Result = [-2147450880..2147483647]
s32Temp1  = [0..2147483646],0%2
return    = full u16 domain

تابع هدف و eva_main دارای normal final state بودند و propagation پس از فراخوانی ادامه پیدا کرد.

Baseline boundary

هیچ تغییری در ضرب اولیه، بررسی denominator، division، multiplication، محاسبه remainder، left shift، addition، saturation، branchها یا returnها انجام نشد.
