# s16_div_s32_s16

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_s16_div_s32_s16.c`  
machdep: `ppc_32`

## Analyzed interface

`s16 s16_div_s32_s16(s32 numerator, s16 denominator);`

## Input model

هر دو پارامتر با کل دامنه نوع خود مدل شدند:

```text
numerator   = -2147483648..2147483647
denominator = -32768..32767

Builtinهای استفاده شده:

Frama_C_long_interval(-2147483647L - 1L, 2147483647L)
Frama_C_short_interval(-32768, 32767)

مقدار صفر از دامنه denominator حذف نشد و مقدار INT32_MIN نیز در دامنه numerator باقی ماند.

هیچ assumption، cast، mask یا محدودسازی اضافه ای در Harness اعمال نشد.

Baseline result

Parse با موفقیت و بدون warning یا error انجام شد.

Eva برای تابع هدف و Harness normal final state تولید کرد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

25 alarms generated by the analysis.

ترکیب alarmها:

17 integer overflows
6 divisions by zero
2 invalid shifts
Alarm locations
Line 274

هشت alarm برای division، multiplication، subtraction و left shift ثبت شد:

division by zero
signed overflow در division
دو signed overflow برای multiplication
invalid operand برای left shift
دو signed overflow برای subtraction
signed overflow برای نتیجه left shift
Line 275

یک alarm مربوط به division by zero ثبت شد.

Line 276

سه alarm ثبت شد:

division by zero
دو signed overflow برای addition
Line 297

هشت alarm برای division، multiplication، subtraction و left shift ثبت شد:

division by zero
signed overflow در division
دو signed overflow برای multiplication
invalid operand برای left shift
دو signed overflow برای subtraction
signed overflow برای نتیجه left shift
Line 298

یک alarm مربوط به division by zero ثبت شد.

Line 299

چهار alarm ثبت شد:

division by zero
signed overflow در division
دو signed overflow برای addition
Final-state note

Eva final state زیر را برای تابع هدف ثبت کرد:

s32Temp1 = full s32 domain
s32Temp2 = [0..2147483646],0%2
return   = full s16 domain

مقدار بازگشتی تابع و متغیر result در Harness به interval محدودتری از دامنه نوع خود محدود نشدند.

Propagation ادامه پیدا کرد و eva_main با مقدار بازگشتی صفر به normal final state رسید.

Baseline boundary

این گزارش وضعیت implementation فعلی را ثبت می کند.

هیچ تغییری در موارد زیر انجام نشد:

بررسی denominator صفر
حالت خاص INT32_MIN
حالت denominator برابر -1
division
multiplication
subtraction
left shift
saturation
branchها
returnها
