# u32_div_s32_s32

## Scope

P1

source: `src_analysis/sources/div_Ccode.c`  
header: `src_analysis/headers/div_Ccode.h`  
harness: `harnesses/P1/eva_main_u32_div_s32_s32.c`  
machdep: `ppc_32`

## Analyzed interface

`u32 u32_div_s32_s32(s32 numerator, s32 denominator);`

## Input model

هر دو پارامتر با کل دامنه نوع `s32` مدل شدند:

```text
numerator   = -2147483648..2147483647
denominator = -2147483648..2147483647

برای هر ورودی از builtin زیر استفاده شد:

Frama_C_long_interval(-2147483647L - 1L, 2147483647L)

مقدار صفر از دامنه denominator و مقدار INT32_MIN از دامنه هیچ‌یک از ورودی‌ها حذف نشدند.

هیچ assumption، cast، mask یا محدودسازی اضافه‌ای در Harness اعمال نشد.

Baseline result

Parse با موفقیت و بدون warning یا error انجام شد.

Eva normal final state برای تابع هدف و Harness تولید کرد.

Summary رسمی Eva نتیجه زیر را ثبت کرد:

9 alarms generated by the analysis.

ترکیب alarmها:

6 integer overflows
2 invalid shifts
1 division by zero
Alarm details
Line 1168

عبارت محاسبه remainder، multiplication و left shift سه alarm ایجاد کرد:

امکان signed overflow در ضرب quotient در denominator
امکان منفی‌بودن operand سمت چپ left shift
امکان signed overflow در نتیجه left shift با مقدار ثابت 1
Line 1182

عبارت زیر برای کل دامنه ورودی alarm signed overflow ایجاد کرد:

numerator = -numerator;

دامنه ورودی شامل INT32_MIN است که منفی‌کردن آن در نوع s32 قابل نمایش نیست.

Line 1183

عبارت زیر نیز alarm signed overflow ایجاد کرد:

denominator = -denominator;

دامنه ورودی denominator شامل INT32_MIN است.

Line 1184

محاسبه division، remainder و left shift چهار alarm ایجاد کرد:

امکان division by zero
امکان signed overflow در multiplication
امکان منفی‌بودن operand سمت چپ left shift
امکان signed overflow در نتیجه left shift

بررسی اولیه denominator == 0 در تابع وجود دارد، اما Eva در مسیر محاسباتی خط 1184 همچنان alarm مربوط به division by zero ثبت کرد.

Final-state note

Eva بازه‌های زیر را برای متغیرهای میانی گزارش کرد:

u32Temp1 = [0..2147483646],0%2
u32Temp2 = [0..4294967293]

مقدار بازگشتی نهایی تابع و متغیر result در Harness به بازه‌ای محدودتر از دامنه نوع خود محدود نشدند.

Propagation متوقف نشد و eva_main به normal final state با مقدار بازگشتی صفر رسید.

Baseline boundary

این Baseline فقط وضعیت implementation فعلی را ثبت می‌کند.

هیچ تغییری در موارد زیر انجام نشد:

بررسی denominator صفر
unary minusها
division
multiplication
محاسبه remainder
left shiftها
branchها
returnها
